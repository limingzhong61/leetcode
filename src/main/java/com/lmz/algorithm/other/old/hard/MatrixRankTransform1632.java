package com.lmz.algorithm.other.old.hard;

/**
 * @author: limingzhong
 * @create: 2023-01-25 10:52
 */
public class MatrixRankTransform1632 {

    ///**
    // * 1 <= m, n <= 500
    // * <p>
    // * bfs
    // */
    //public int[][] matrixRankTransform(int[][] matrix) {
    //    int m = matrix.length, n = matrix[0].length;
    //    //移动数组
    //    int fourDir[][] = new int[][]{
    //            {-1, 0}, {0, 1}, {1, 0}, {0, -1}
    //    };
    //    for (int i = 0; i < m; i++){
    //        for(int j = 0; j < n; j++){
    //            boolean smallest = true;
    //            for(var dir : fourDir){
    //                int nextI = i + dir[0];
    //                int nextJ = j + dir[1];
    //                if(nextI >= 0 && nextI < m &&nextJ >= 0 && nextJ < n ){
    //                    if(matrix[nextI][nextJ] < matrix[i][j]){
    //
    //                    }
    //                }
    //            }
    //        }
    //    }
    //
    //}
}
